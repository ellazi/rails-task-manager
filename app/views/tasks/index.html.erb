<h1>My tasks</h1>
<ul>
  <% @tasks.each do |t| %>
    <li><p class="checkbox"><%= t.completed ? "[x]" : "[ ]" %><p><%= link_to t.title, task_path(t) %></p></li>
  <% end %>
</ul>

<p class="button"><%= link_to "Add a new task", tasks_new_path %></p>
